# Nightly Health Check — 2026-09-04

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding).** This sandbox's outbound egress
  policy blocks the CONNECT to `ndjgvmvfgtiuchwlpqed.supabase.co` outright (403 policy denial at
  the proxy — `recentRelayFailures` showed `connect_rejected` for that host before any request
  reached Supabase), so the anonymous signup + `check-price` call could not be attempted tonight.
  This is an environment restriction on this nightly-check session, not evidence the live app or
  Edge Function is broken. Per this log directory's history, this exact block has now prevented
  the live smoke test on **every single nightly run since the check began on 2026-07-27 — 40
  consecutive nights** — the check has never once confirmed the live backend is healthy. Still
  needs either (a) a manual run of the smoke test from an unrestricted environment, or (b) an
  update to this session's egress allowlist to include the Supabase project host, or this
  section of the nightly check should be considered non-functional.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules bundled, 1.2MB JS
  bundle, favicon.ico + index.html + metadata.json). Generated `dist/` deleted after the check, not
  committed.
- **npm audit:** 26 vulnerabilities (18 moderate, 8 high, 0 critical) — unchanged from 2026-09-03.
  High-severity: `brace-expansion`, `image-size` (→ `metro`, `metro-config`,
  `metro-transform-worker`, `@expo/metro`), `js-yaml`, `nanoid`. Moderate: `uuid` (→
  `@expo/ngrok`, `xcode`, `@expo/config-plugins`, `@expo/cli`, `expo`, `@expo/config`,
  `@expo/local-build-cache-provider`, `@expo/metro-config`, `@expo/inline-modules`,
  `@expo/prebuild-config`), `decode-uri-component` (→ `query-string`, `@react-navigation/core`,
  `@react-navigation/native`, `@react-navigation/elements`, `@react-navigation/native-stack`),
  `@xmldom/xmldom` (XML fragment injection via invalid `EntityReference.nodeName`). All still
  trace to the Expo CLI/Metro dev-and-build toolchain, not app runtime code. Plain
  `npm audit fix` doesn't clear the high-severity set; `npm audit fix --force` would downgrade
  `expo` to a breaking pre-40 version (46.0.21) — not attempted, still needs human review.
- **Outdated deps:** None more than one major version behind. Exactly one major behind:
  `typescript` 6.0.3 → 7.0.2, `@react-native-async-storage/async-storage` 2.2.0 → 3.1.1 (unchanged
  from prior nights). Everything else outdated is a minor/patch bump (`expo` 57.0.8→57.0.19,
  `@supabase/supabase-js` 2.110.8→2.115.0, `react-native` 0.86.0→0.87.1, `@react-navigation/native`
  7.3.14→7.3.18, `@react-navigation/native-stack` 7.18.6→7.18.10, `react`/`react-dom`
  19.2.3→19.2.8, `expo-image-picker` 57.0.6→57.0.15, `react-native-screens` 4.26.2 (Wanted;
  Latest 4.27.0), `react-native-svg` 15.15.4→15.15.5, `react-native-safe-area-context`
  5.7.0→5.9.1, `@types/react` 19.2.17→19.2.18).
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX/HACK comments found in `app/src` or
  `supabase`. One `console.log` in `supabase/scripts/sync-baselines.ts` (a manual CLI sync
  script, not runtime app/function code) — informational status output, not a debug leftover. No
  stray backup/tmp/debug files found under the project (excluding `node_modules` and the
  gitignored `.expo/` cache generated by tonight's own `expo export` run).
