# Nightly Health Check — 2026-09-10

## Status: ISSUES FOUND

- **Live backend smoke test:** COULD NOT RUN — this session's network egress policy
  blocked outbound HTTPS to `ndjgvmvfgtiuchwlpqed.supabase.co` (proxy returned 403,
  "gateway answered 403 to CONNECT (policy denial or upstream failure)"). This is an
  environment/network policy restriction on this scheduled session, not a confirmed
  failure of the live app or backend — the endpoint was never actually reached, so its
  real status is unknown. A human should re-run the same signup + `check-price` call
  (see step 2 of the nightly prompt) from an environment with Supabase egress allowed,
  or adjust this session's egress policy to permit that host.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules,
  1.2MB JS bundle). Generated `dist/` was deleted after the check per instructions.
- **npm audit:** 26 vulnerabilities (17 moderate, 9 high), all transitive, rooted in
  `@expo/metro`/`metro-config`/`metro-transform-worker`, `js-yaml`, `nanoid`, and
  `uuid` (pulled in via `@expo/ngrok`, `xcode`, `@expo/config-plugins`, `@expo/cli`,
  `expo`). `npm audit fix` covers `js-yaml`/`nanoid`; the `uuid` chain needs
  `npm audit fix --force`, which would downgrade `expo` to `46.0.21` (breaking) — not
  a safe automatic fix given the app is currently on Expo 57.
- **Outdated deps:** No dependency is more than one major version behind. Closest:
  `@react-native-async-storage/async-storage` 2.2.0 → latest 3.1.1 (one major),
  `typescript` 6.0.3 → latest 7.0.2 (one major). Everything else (`expo`,
  `react-navigation`, `@supabase/supabase-js`, `react`, `react-dom`, `react-native`,
  `expo-image-picker`, `react-native-svg`, `react-native-screens`,
  `react-native-safe-area-context`, `@types/react`) is only a patch/minor behind.
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX/HACK comments, no stray
  `console.log`/`debugger` statements, no backup/debug-leftover files, and no
  uncommitted files under `app/src` or `supabase/functions`. Only `.env` file present
  is the tracked `app/.env.example` template (no real secrets).
