# Nightly Health Check — 2026-08-08

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding).** The sandbox's outbound proxy
  rejected the `CONNECT` to `ndjgvmvfgtiuchwlpqed.supabase.co:443` with a hard 403 (confirmed via
  `curl` on the signup call and via the proxy's own status endpoint, which logs it as
  `connect_rejected` / policy denial). The anon signup + `check-price` function call could not be
  attempted at all — no `estimate` object was ever fetched, pass or fail. This is now **13
  consecutive nights** (every dated log from 2026-07-27 through tonight) that this check has never
  once actually run. The routine currently provides zero real signal on whether live users can get
  a price estimate. This remains an environment allowlist gap (`ndjgvmvfgtiuchwlpqed.supabase.co`
  or `*.supabase.co` needs adding to this scheduled task's network policy, or the smoke test needs
  to move to an environment that has it) — not a confirmed app/backend outage — but nearly two
  weeks unresolved is worth escalating on its own.
- **Type check:** PASS — `npx tsc --noEmit` completed with no errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS
  bundle). Generated `dist/` deleted after export, not committed.
- **npm audit:** 22 vulnerabilities (8 moderate, 14 high). All are in Expo/Metro/React Native
  dev-tooling chains (`@expo/cli`, `@expo/metro*`, `metro*`, `@react-native/community-cli-plugin`,
  `xcode`, `uuid`), not app runtime code. Notable high-severity entries: `image-size` (DoS via
  infinite loop in ICNS/JXL/HEIF parsing), `js-yaml` (quadratic CPU DoS, CVE-2026-59870 mitigation
  not backported to this version), `nanoid` (infinite loop with size-zero custom generator),
  `brace-expansion` (DoS via unbounded intermediate arrays). `npm audit fix --force` is available
  but downgrades `expo` (breaking) — needs human review, not auto-applied.
- **Outdated deps:** `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1) and `typescript`
  (6.0.3 → 7.0.2) are each one major version behind. Everything else outdated is a patch/minor
  bump (`@supabase/supabase-js`, `expo`, `expo-image-picker`, `react`, `react-dom`, `react-native`,
  `react-native-safe-area-context`, `react-native-screens`, `react-native-svg`, `@types/react`,
  `@react-navigation/native`, `@react-navigation/native-stack`). Nothing is more than one major
  version behind.
- **Housekeeping notes:** No TODO/FIXME/console.log/debugger found under `app/` or
  `supabase/functions`. No stray backup/debug/tmp files in the repo tree (only the expected,
  gitignored `.expo/` build cache). `git status` was clean before and after this run. Same doc/code
  drift as prior runs, still unfixed: `README.md` still describes `claude-opus-4-8` as the default
  price-search model, but `supabase/functions/check-price/index.ts` (`MODEL` constant, line 15)
  already uses `claude-sonnet-5`.
