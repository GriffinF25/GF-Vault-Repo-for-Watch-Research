# Nightly Health Check — 2026-07-27

## Status: ISSUES FOUND

- **Live backend smoke test: FAIL (not run) — blocked by this environment's network egress policy.**
  The sandbox's outbound proxy returned 403 on `CONNECT` to `ndjgvmvfgtiuchwlpqed.supabase.co:443`,
  so the anon signup + `check-price` function call could not be attempted at all. This is an
  environment/network-policy limitation of the nightly-check sandbox, not a confirmed app or
  backend failure — the actual Supabase project's health is unverified tonight. Needs a run from
  an environment whose egress policy allows the Supabase host (or an allowlist addition for
  `*.supabase.co`).
- Type check: PASS — `npx tsc --noEmit` completed with no errors.
- Web bundle: PASS — `npx expo export --platform web` completed successfully (Metro bundled 608
  modules, output `dist/` generated and deleted afterward per instructions; not committed).
- npm audit: 11 moderate-severity vulnerabilities, all transitive via `uuid` (<11.1.1, missing
  buffer bounds check, GHSA-w5hq-g745-h8pq) pulled in through Expo CLI's dev-tooling chain
  (`@expo/ngrok`, `xcode`, `@expo/config-plugins`, `@expo/cli`, `expo`). No direct/production
  runtime dependency is affected. `npm audit fix --force` is available but would downgrade `expo`
  to 46.0.21 (a major regression) — not recommended without manual review.
- Outdated deps: none more than one major version behind. One major behind: `typescript`
  (6.0.3 → 7.0.2 latest) and `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1 latest).
  Everything else (`react`, `react-dom`, `react-native-safe-area-context`, `react-native-svg`) is
  only a minor/patch behind.
- Housekeeping notes: no TODO/FIXME/XXX/HACK comments, no `console.log`/`debugger` statements,
  and no stray debug/backup/tmp files found under `app/src` or `supabase`. `npm install` and
  `npm outdated` ran clean. No unrelated working-tree changes were touched.
