# Nightly Health Check — 2026-08-07

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding).** This environment's egress
  policy still rejects the CONNECT to `ndjgvmvfgtiuchwlpqed.supabase.co:443` with a hard 403
  (confirmed directly, not just via the earlier `npx expo export` failure path — same 403 seen
  on a bare `curl -v` to `/auth/v1/health`). The anon signup + `check-price` function call could
  not be attempted at all. This is now **twelve nights in a row** (since 2026-07-27) that this
  check has never once executed — it is providing zero real signal on whether the live app
  actually works for end users. This is an environment allowlist gap (`ndjgvmvfgtiuchwlpqed.supabase.co`
  or `*.supabase.co` needs adding to this scheduled task's network policy, or the smoke test
  needs to move to an environment that has it), not a confirmed app-side outage — but it has gone
  unresolved for going on two weeks and deserves attention independent of tonight's other findings.
- **Type check:** PASS — `npx tsc --noEmit` completed with no errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS
  bundle). Generated `dist/` deleted after export, not committed.
- **npm audit:** 13 vulnerabilities (11 moderate, 2 high) — one more high-severity finding than
  last night. New: `js-yaml` 4.0.0–4.3.0 (high, via `expo` → `@expo/cli` → `@expo/xcpretty` →
  `js-yaml`), fix available without a breaking change. Still present: `brace-expansion` (high,
  DoS via unbounded intermediate arrays, non-breaking fix available). The 11 moderate findings
  are the same recurring `uuid <11.1.1` chain via `@expo/ngrok` → `xcode` → `@expo/config-plugins`
  → `@expo/cli` → `expo` (dev/build tooling, not app runtime code) — resolving those still
  requires `--force`, which would downgrade `expo` to `46.0.21` (breaking), so needs human
  judgment rather than auto-applying.
- **Outdated deps:** `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1) and `typescript`
  (6.0.3 → 7.0.2) are each one major version behind. Everything else outdated is a patch/minor
  bump (`@supabase/supabase-js`, `expo`, `expo-image-picker`, `react`, `react-dom`,
  `react-native`, `react-native-safe-area-context`, `react-native-svg`, `@types/react`,
  `@react-navigation/native`, `@react-navigation/native-stack`). Nothing is more than one major
  version behind.
- **Housekeeping notes:** No TODO/FIXME/XXX/HACK comments or `console.log`/`console.debug` calls
  found under `app/src` or `supabase/functions`. No stray backup/debug/scratch files in the repo
  tree (only the expected `.expo/dev/logs/export.log` build artifact, which is gitignored and
  untracked). `git status` was clean before and after this run. Same doc/code drift as prior
  runs, still unfixed: `README.md` still describes `claude-opus-4-8` as the default price-search
  model, but `supabase/functions/check-price/index.ts` (`MODEL` constant, line 15) already uses
  `claude-sonnet-5`.
