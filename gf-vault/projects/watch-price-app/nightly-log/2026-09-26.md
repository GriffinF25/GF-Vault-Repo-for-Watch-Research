# Nightly Health Check — 2026-09-26

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding, ~62 consecutive nights now).**
  This session's outbound network policy blocks the CONNECT to
  `ndjgvmvfgtiuchwlpqed.supabase.co:443` at the egress proxy — `curl -X POST
  {url}/auth/v1/signup` failed immediately with `CONNECT tunnel failed, response 403`, before
  any request reached Supabase. Per this log directory's history, every single nightly run
  back to the first one (2026-07-27) has hit this identical block — the live `check-price`
  path has never once been verified from this sandbox. This is an environment/egress-policy
  limitation on the nightly-check session itself, **not** an observed app or backend failure —
  do not read it as confirmation the backend is broken, but it also means two months of nightly
  runs have provided zero real signal on whether `check-price` is actually serving estimates to
  users. Worth escalating: either allowlist `*.supabase.co` for this session's egress policy, or
  run this specific check from an environment that has it, so it can finally return a real
  pass/fail.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` completed successfully (608 modules,
  1.2MB JS bundle, exported to `dist/`). `dist/` was deleted after the check; nothing committed.
- **npm audit:** 19 vulnerabilities (5 high, 14 moderate). Same magnitude/character as recent
  nights — transitive build/dev tooling pulled in via Expo's CLI dependency chain, not app
  runtime code shipped to users.
- **Outdated deps:** No dependency more than one major version behind except two: `typescript`
  (6.0.3 → 7.0.2) and `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1). Everything
  else (expo, react, react-dom, react-native, react-navigation, supabase-js, react-native-svg,
  react-native-web, etc.) is only patch/minor behind latest.
- **Housekeeping notes:** No TODO/FIXME/XXX/HACK comments under `app/src` or `supabase`. No
  stray debug/backup/tmp files in the project tree (only a `.expo/dev/logs/export.log`
  generated by tonight's own export run, inside the gitignored `.expo/` directory — not
  tracked, not an issue). One `console.log` in `supabase/scripts/sync-baselines.ts` — a
  legitimate CLI status line for that sync script, not debug leftover. No `.env` files present
  to leak (only the committed `.env.example`). `git status` on the project tree is clean.

## Action needed
Nothing in the app itself failed tonight — type check, bundle, and dependency posture are all
healthy and consistent with recent nights. The recurring open item is procedural but now
significant by duration: this session has never been able to reach Supabase in ~2 months of
nightly runs, so the one check that matters most (does `check-price` actually return a live
estimate to a real user) remains completely unverified. Recommend fixing the egress policy for
this scheduled session, or moving this specific check to a session/environment that can reach
`*.supabase.co`.
