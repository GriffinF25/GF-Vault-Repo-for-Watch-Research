# Nightly Health Check — 2026-08-28

## Status: ISSUES FOUND

- **Live backend smoke test: UNVERIFIED (not a confirmed app failure).** This session's
  outbound network policy blocks the sandbox from reaching `ndjgvmvfgtiuchwlpqed.supabase.co`
  at all (proxy log shows a 403 policy denial on the CONNECT, before any request left the
  sandbox) — so the signup + `check-price` call in step 2 of the runbook could not be
  attempted tonight. This is an environment restriction on this nightly-check session, not
  evidence the live app or Edge Function is broken. **Needs a manual run from an unrestricted
  environment** to actually confirm `check-price` is healthy.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules bundled,
  1.2MB JS bundle). Generated `dist/` deleted after the check, not committed.
- **npm audit:** 19 vulnerabilities (11 moderate, 8 high). All trace back to a vulnerable
  `uuid` range pulled in transitively via `@expo/ngrok` → `xcode` → `@expo/config-plugins` →
  the Expo CLI toolchain (dev/build tooling, not app runtime code). `npm audit fix` alone
  doesn't resolve it; `npm audit fix --force` would downgrade `expo` to 46.0.21 (breaking) —
  not attempted, flagged for a human to weigh.
- **Outdated deps:** None more than one major version behind. One-major-behind: `typescript`
  6.0.3 → 7.0.2, `@react-native-async-storage/async-storage` 2.2.0 → 3.1.1. Everything else
  outdated is a minor/patch bump (`expo`, `@supabase/supabase-js`, `react-native`,
  `@react-navigation/*`, `react`/`react-dom`, `expo-image-picker`, etc.).
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX, `console.log`, or `debugger` left in
  `app/src` or `supabase/functions`. No stray debug/backup/tmp files under the project. The
  only log-like file found (`app/.expo/dev/logs/export.log`) is inside the gitignored
  `.expo/` build-cache dir, not a real leftover.
