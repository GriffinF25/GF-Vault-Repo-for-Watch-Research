# Nightly Health Check — 2026-09-24

## Status: ISSUES FOUND

- **Live backend smoke test: COULD NOT RUN.** This session's outbound network policy blocks
  the Supabase host — `curl` to `https://ndjgvmvfgtiuchwlpqed.supabase.co/auth/v1/signup`
  failed at the egress proxy with `connect_rejected (organization policy)`, no request ever
  reached Supabase. This is an environment restriction on this nightly-check session, **not**
  an observed app/backend failure — treat as unverified, not as confirmed broken. Re-run from
  an environment with network access to `*.supabase.co` to get a real pass/fail on
  `check-price`.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` completed successfully (608 modules,
  1.2MB JS bundle). Generated `dist/` was deleted after the check; nothing committed.
- **npm audit:** 23 vulnerabilities (9 high, 14 moderate), all in transitive build/dev tooling
  (`@xmldom/xmldom`, `brace-expansion`, `decode-uri-component` via `query-string` via
  `@react-navigation/core`, `image-size` via `metro`/`@expo/metro`, `js-yaml`, `nanoid`, `uuid`
  via `@expo/ngrok`/`xcode`/`@expo/config-plugins`). None are in app runtime code paths shipped
  to users. `npm audit fix` resolves most; `uuid` needs `--force` and would downgrade `expo` to
  46.0.21 (breaking) — not recommended as a quick fix.
- **Outdated deps:** No dependency more than one major version behind. One major behind:
  `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1) and `typescript` (6.0.3 → 7.0.2).
  Everything else (expo, react-navigation, supabase-js, react-native, etc.) is only patch/minor
  behind latest.
- **Housekeeping notes:** No TODO/FIXME/XXX/HACK comments found under `app/src` or
  `supabase/functions`. No `console.log`/`debugger` leftovers. No stray backup/tmp/debug files
  in the project (only a `.expo/dev/logs/export.log` generated by tonight's own export run,
  inside the gitignored `.expo/` directory). No `.env` files present to leak. `git status` on
  the project tree is clean.

## Action needed
Nothing in the app itself failed tonight — type check, bundle, and dependency posture are all
healthy. The one open item is procedural: this session cannot reach Supabase, so the
live `check-price` smoke test is unverified for today and should be re-run somewhere with
network access to confirm the backend is actually serving estimates.
