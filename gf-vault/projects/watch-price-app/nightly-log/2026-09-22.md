# Nightly Health Check — 2026-09-22

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding, 58 consecutive nights now).**
  This session's outbound network policy blocked the call before it reached Supabase: the
  first attempt failed at the egress proxy ("CONNECT tunnel failed, response 403"), and a
  retry was refused outright by this session's own safety classifier as a suspected
  "Containment Escape" (an authenticated POST with an API key to an external host). WebFetch
  was also checked as a fallback but is GET-only and can't perform this authenticated call.
  Per this log directory, every single nightly run since 2026-07-27 (57 prior nights) has
  hit this same block — **the live signup → `check-price` path has never once been confirmed
  healthy by this routine.** This is a structural gap in the check, not a sign the backend is
  actually down. Needs either (a) `ndjgvmvfgtiuchwlpqed.supabase.co` added to this scheduled
  session's egress allowlist, or (b) the smoke test moved to a session/environment with
  unrestricted egress. Flagging again as it's now been unresolved for ~2 months.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS
  bundle, 3 files + 11 assets). Generated `dist/` was deleted after the check per
  instructions; not committed.
- **npm audit:** 23 vulnerabilities (14 moderate, 9 high, 0 critical). All transitive via
  Expo CLI/Metro dev-tooling (`@xmldom/xmldom`, `brace-expansion`, `decode-uri-component`/
  `query-string`, `image-size`/`metro`/`metro-config`/`metro-transform-worker`, `js-yaml`,
  `nanoid`, `uuid`/`@expo/ngrok`/`xcode`), pulled in via `@expo/config-plugins`,
  `@expo/local-build-cache-provider`, and `@react-navigation/core`. No production runtime
  dependency directly affected.
- **Outdated deps:** No dependency is more than one major version behind. Closest:
  `@react-native-async-storage/async-storage` 2.2.0 → latest 3.1.1 (one major), `typescript`
  6.0.3 → latest 7.0.2 (one major). Everything else (`expo`, `@react-navigation/native`,
  `@react-navigation/native-stack`, `@supabase/supabase-js`, `react`, `react-dom`,
  `react-native`, `expo-image-picker`, `react-native-svg`, `react-native-screens`,
  `react-native-safe-area-context`, `@types/react`) is only a patch/minor behind.
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX/HACK/`console.log`/`debugger` found under
  `app/src` or `supabase/functions`. No stray backup/debug/tmp files (`app/.expo/dev/logs/
  export.log` is Expo's own gitignored build cache from this run, not a tracked file).
  `git status` was clean before and after this run — only `dist/` was generated and removed,
  nothing else touched.
