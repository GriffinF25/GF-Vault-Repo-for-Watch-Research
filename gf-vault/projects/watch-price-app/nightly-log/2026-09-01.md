# Nightly Health Check — 2026-09-01

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding).** This sandbox's outbound egress
  policy blocks the CONNECT to `ndjgvmvfgtiuchwlpqed.supabase.co` outright (403 policy denial at
  the proxy — `recentRelayFailures` shows `connect_rejected` for that host before any request
  reached Supabase), so the anonymous signup + `check-price` call could not be attempted tonight.
  This is an environment restriction on this nightly-check session, not evidence the live app or
  Edge Function is broken. Per this log directory's history, this exact block has now prevented
  the live smoke test on every nightly run since at least 2026-07-27 (36+ consecutive nights) —
  the check has never once confirmed the live backend is healthy in that span. Still needs either
  (a) a manual run of the smoke test from an unrestricted environment, or (b) an update to this
  session's egress allowlist to include the Supabase project host.
- **Local clone quirk, self-corrected (not a repo issue).** This session started in a detached
  HEAD, and this container's local `main` branch ref was stale (pointing at 2026-08-27's commit)
  even though GitHub's actual `origin/main` already had all four nights of logs through 08-31. A
  `git push` from that stale local `main` was correctly rejected as non-fast-forward; running
  `git fetch origin main` confirmed all four nights' logs are present and correct on GitHub, then
  `git rebase origin/main` brought tonight's commit onto the real tip before pushing. No data was
  ever missing from the shared repo — this was purely a stale ref in this session's local clone.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules bundled, 1.2MB JS
  bundle). Generated `dist/` deleted after the check, not committed.
- **npm audit:** 25 vulnerabilities (17 moderate, 8 high, 0 critical) — up from 19 (11 moderate, 8
  high) on 2026-08-31. High-severity findings: `@expo/metro`, `metro`, `metro-config`,
  `metro-transform-worker`, `js-yaml`, `nanoid`, plus two new entries not seen in recent nights,
  `brace-expansion` and `image-size`. Moderate findings otherwise unchanged in kind (`@expo/cli`,
  `@expo/config`, `@expo/config-plugins`, `@expo/ngrok`, `xcode`, `uuid`, `@react-navigation/*`,
  `query-string`, `decode-uri-component`). All still trace to the Expo CLI/Metro dev-and-build
  toolchain, not app runtime code. Plain `npm audit fix` doesn't clear the high-severity set;
  `npm audit fix --force` would downgrade `expo` to a breaking pre-40 version — not attempted,
  still needs human review. Worth a look given the jump from 19 to 25 findings since yesterday.
- **Outdated deps:** None more than one major version behind. Exactly one major behind:
  `typescript` 6.0.3 → 7.0.2, `@react-native-async-storage/async-storage` 2.2.0 → 3.1.1 (unchanged
  from prior nights). Everything else outdated is a minor/patch bump (`expo` 57.0.8→57.0.18,
  `@supabase/supabase-js` 2.110.8→2.112.4, `react-native` 0.86.0→0.87.1, `@react-navigation/native`
  7.3.14→7.3.18, `@react-navigation/native-stack` 7.18.6→7.18.10, `react`/`react-dom`
  19.2.3→19.2.8, `expo-image-picker` 57.0.6→57.0.14, `react-native-screens` 4.26.2→4.26.2 (Wanted;
  Latest 4.27.0), `react-native-svg` 15.15.4→15.15.5, `react-native-safe-area-context`
  5.7.0→5.9.1, `@types/react` 19.2.17→19.2.18).
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX/HACK comments or `console.log`/`debugger` left
  in `app/src` or `supabase/functions`. No stray backup/tmp/debug files found under the project
  (excluding `node_modules`).
