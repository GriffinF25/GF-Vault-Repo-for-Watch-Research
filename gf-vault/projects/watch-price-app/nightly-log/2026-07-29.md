# Nightly Health Check — 2026-07-29

## Status: ISSUES FOUND

- **Live backend smoke test: FAIL (not run) — blocked by this environment's network egress policy,
  third consecutive night.** The sandbox's outbound proxy returned 403 on `CONNECT` to
  `ndjgvmvfgtiuchwlpqed.supabase.co:443`, so the anon signup + `check-price` function call could
  not be attempted at all (confirmed via `$HTTPS_PROXY/__agentproxy/status`, which logged a fresh
  `connect_rejected` denial for this exact host at the time of the attempt). This is the same
  failure as 2026-07-27 and 2026-07-28 — the actual Supabase project's live health is
  **unverified for three nights running**. This remains an environment/network-policy limitation
  of the nightly-check sandbox, not a confirmed app or backend failure. Needs either an allowlist
  addition for `*.supabase.co` on this scheduled session's egress policy, or a manual live check
  from an unrestricted environment.
- Type check: PASS — `npx tsc --noEmit` completed with no errors.
- Web bundle: PASS — `npx expo export --platform web` completed successfully (Metro bundled 608
  modules in ~15s, output `dist/` generated and deleted afterward per instructions; not
  committed).
- npm audit: 11 moderate-severity vulnerabilities, all transitive via `uuid` (<11.1.1, missing
  buffer bounds check, GHSA-w5hq-g745-h8pq) pulled in through Expo CLI's dev-tooling chain
  (`@expo/ngrok`, `xcode`, `@expo/config-plugins`, `@expo/cli`, `expo`). No direct/production
  runtime dependency is affected. `npm audit fix --force` is available but would downgrade `expo`
  to 46.0.21 (a major regression) — not recommended without manual review. Unchanged from
  2026-07-27/28.
- Outdated deps: none more than one major version behind. One major behind: `typescript`
  (6.0.3 → 7.0.2 latest) and `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1 latest).
  Everything else (`@supabase/supabase-js`, `react`, `react-dom`, `react-native`,
  `react-native-safe-area-context`, `react-native-svg`) is only a minor/patch behind. Unchanged
  from 2026-07-27/28.
- Housekeeping notes: no TODO/FIXME/XXX/HACK comments, no `console.log`/`debugger` statements, no
  stray debug/backup/tmp files under `app/src` or `supabase`, and no `.env`/secret files
  accidentally tracked in git. `npm install` ran clean (575 packages). No unrelated working-tree
  changes were touched.
