# Nightly Health Check — 2026-08-14

## Status: ISSUES FOUND

- **Live backend smoke test: FAIL — not executed (top-line finding).** The sandbox's outbound
  proxy rejected the `CONNECT` to `ndjgvmvfgtiuchwlpqed.supabase.co:443` with a hard 403
  (`curl`: "CONNECT tunnel failed, response 403"; HTTP status `000`). The proxy's own status
  endpoint confirms the rejection was a policy denial (`connect_rejected`, "gateway answered 403
  to CONNECT (policy denial or upstream failure)") — `supabase.co` is still not in this
  environment's egress allowlist. The anon signup call could not be attempted, so `check-price`
  and its `estimate` payload were never reached — no pass/fail signal on the backend itself.
  This is now **19 consecutive nights** (2026-07-27 through tonight) this check has never once
  run. Still an environment allowlist gap, not a confirmed app/backend outage —
  `ndjgvmvfgtiuchwlpqed.supabase.co` (or `*.supabase.co`) needs adding to this scheduled task's
  network policy. The routine cannot fix this itself and will keep reporting it nightly until a
  human changes the environment config.
- **Type check:** PASS — `npx tsc --noEmit` completed with no errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS
  bundle). Generated `dist/` deleted after export, not committed.
- **npm audit:** 22 vulnerabilities (8 moderate, 14 high) — unchanged from 2026-08-13. All are in
  Expo/Metro/React Native dev-tooling chains (`@expo/cli`, `@expo/metro*`, `metro*`,
  `@react-native/community-cli-plugin`, `@react-native/virtualized-lists`, `expo`, `react-native`,
  `xcode`, `uuid`), not app runtime code. Same notable high-severity entries as before:
  `image-size` (DoS via infinite loop in ICNS/JXL/HEIF parsing), `js-yaml` (quadratic CPU DoS,
  CVE-2026-59870 fix not backported), `nanoid` (infinite loop with size-zero custom generator),
  `brace-expansion` (DoS via unbounded intermediate arrays). `npm audit fix --force` is available
  but downgrades `expo` (breaking) — still needs human review, not auto-applied.
- **Outdated deps:** Same two one-major-behind packages as prior nights —
  `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1) and `typescript` (6.0.3 → 7.0.2).
  Neither is more than one major version behind. Everything else outdated is a patch/minor bump
  (`@supabase/supabase-js`, `expo`, `expo-image-picker`, `react`, `react-dom`, `react-native`,
  `react-native-safe-area-context`, `react-native-screens`, `react-native-svg`, `@types/react`,
  `@react-navigation/native`, `@react-navigation/native-stack`). Nothing newly crossed the
  one-major-version threshold.
- **Housekeeping notes:** No TODO/FIXME/XXX, `console.log`, or `debugger` found anywhere under
  the tracked `watch-price-app/` tree (excluding `node_modules`). No stray backup/debug/tmp files.
  `git status` was clean before and after this run. Same doc/code drift as prior runs, still
  unfixed: `README.md` (line 100) still describes `claude-opus-4-8` as the default price-search
  model, but `supabase/functions/check-price/index.ts` (`MODEL` constant, line 15) already uses
  `claude-sonnet-5`.
