# Nightly Health Check — 2026-09-20

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding).** This session's outbound
  network policy blocks the CONNECT to `ndjgvmvfgtiuchwlpqed.supabase.co:443` — curl failed
  with "CONNECT tunnel failed, response 403" (agent proxy reports `connect_rejected`, "the
  egress proxy denied the CONNECT (organization policy)"). Per this log directory's history,
  this block has prevented the smoke test on every nightly run since 2026-07-27 —
  **56 consecutive nights** — meaning this check has never once confirmed the live backend
  (anon signup → `check-price` function) is actually healthy. This needs either (a)
  `*.supabase.co` added to this scheduled session's egress allowlist, or (b) the smoke test
  moved to a session/environment with unrestricted egress.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS
  bundle, 3 files + 11 assets). Generated `dist/` was deleted after the check per instructions;
  not committed.
- **npm audit:** 23 vulnerabilities (14 moderate, 9 high, 0 critical) — same count as
  2026-09-19. All transitive via Expo CLI/Metro dev-tooling (`uuid` <11.1.1 buffer bounds check,
  `xcode`, `query-string`/`decode-uri-component`, `nanoid`, etc., pulled in through
  `@expo/ngrok`/`@expo/config-plugins`/`@react-navigation/core`); no production runtime
  dependency directly affected.
- **Outdated deps:** No dependency is more than one major version behind. Closest:
  `@react-native-async-storage/async-storage` 2.2.0 → latest 3.1.1 (one major), `typescript`
  6.0.3 → latest 7.0.2 (one major). Everything else (`expo`, `@react-navigation/native`,
  `@react-navigation/native-stack`, `@supabase/supabase-js`, `react`, `react-dom`,
  `react-native`, `expo-image-picker`, `react-native-svg`, `react-native-screens`,
  `react-native-safe-area-context`, `@types/react`) is only a patch/minor behind.
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX/console.log/debugger found under `app/src`
  or `supabase/functions`. No backup/debug/stray files found under either directory. `git
  status` was clean before and after this run (`dist/` removed, nothing else touched).
