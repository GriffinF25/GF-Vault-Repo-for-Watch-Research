# Nightly Health Check — 2026-09-08

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding, 44 nights running).** This session's
  outbound egress policy blocked the CONNECT to `ndjgvmvfgtiuchwlpqed.supabase.co` outright (403
  policy denial at the proxy, confirmed via `recentRelayFailures` in the proxy's status endpoint —
  `cdp.expo.dev` was rejected the same way). The anonymous signup call could not even be
  attempted, so `check-price` was never reached. This is an environment restriction on this
  nightly-check session, not evidence the live app or Edge Function is broken. Per this log
  directory's history, this exact block has now prevented the live smoke test on **every single
  nightly run since the check began on 2026-07-27 — 44 consecutive nights** — this check has
  never once confirmed the live backend is healthy. Still needs either (a) a manual run of the
  smoke test from an unrestricted environment, or (b) an update to this session's egress allowlist
  to include the Supabase project host — this section of the nightly check should otherwise be
  considered non-functional rather than re-reported unchanged indefinitely.

- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.

- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, ~10.7s bundle
  time, 1.2MB JS bundle). Generated `dist/` was deleted afterward per instructions; nothing
  committed.

- **npm audit:** 26 vulnerabilities (18 moderate, 8 high) in the app's `node_modules`.
  - Most (xmldom, brace-expansion, image-size/metro chain, js-yaml, nanoid) have a fix available
    via plain `npm audit fix`.
  - `decode-uri-component` (via `query-string` → `@react-navigation/core` → the
    `@react-navigation/*` packages already in use) has **no fix available** at the currently
    installed major versions.
  - `uuid` (via `@expo/ngrok`/`xcode`/`@expo/config-plugins`, pulled in transitively by `expo`)
    only resolves via `npm audit fix --force`, which would downgrade `expo` to `46.0.21` — a
    major breaking change, not applied tonight (diagnose-only run).

- **Outdated deps:** No dependency is more than one major version behind. Minor/patch bumps
  available: `expo` 57.0.8→57.0.20, `expo-image-picker` 57.0.6→57.0.16, `@supabase/supabase-js`
  2.110.8→2.116.0, `@react-navigation/native` 7.3.14→7.3.18, `@react-navigation/native-stack`
  7.18.6→7.18.10, `react-native-screens` 4.26.2→4.26.2 (wanted matches, latest 4.27.0),
  `@types/react` 19.2.17→19.2.18, `react`/`react-dom` 19.2.3→19.2.8, `react-native`
  0.86.0→0.87.1. One dependency is exactly one major behind (not flagged as "more than one"):
  `@react-native-async-storage/async-storage` 2.2.0→3.1.1. `typescript` 6.0.3→7.0.2 is also
  exactly one major behind.

- **Housekeeping notes:** No TODO/FIXME/XXX comments, `console.log`, or `debugger` statements
  found under `app/src` or `supabase/functions`. No stray `.orig`/`.bak`/`.tmp`/`.DS_Store`
  files found in the project. `git status` was clean before and after this run (only this log
  file is new).
