# Nightly Health Check — 2026-09-25

## Status: ISSUES FOUND

- **Live backend smoke test: COULD NOT RUN.** This session's outbound network policy blocks
  the Supabase host at the egress proxy — `curl -X POST
  https://ndjgvmvfgtiuchwlpqed.supabase.co/auth/v1/signup` failed with `CONNECT tunnel failed,
  response 403` before any request reached Supabase. Same restriction seen on prior nightly-check
  runs (e.g. 2026-09-24). This is an environment limitation on this session, **not** an observed
  app/backend failure — treat `check-price` as unverified tonight, not as confirmed broken. Needs
  re-run from an environment with network access to `*.supabase.co`.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` completed successfully (608 modules,
  1.2MB JS bundle, exported to `dist/`). `dist/` was deleted after the check; nothing committed.
- **npm audit:** 19 vulnerabilities (5 high, 14 moderate), all in transitive dev/build tooling
  pulled in via `expo`'s CLI dependency chain (`@expo/cli` → `@expo/config-plugins` → `xcode` →
  `uuid`, plus `@expo/ngrok` → `uuid`, and `nanoid`). None are in app runtime code shipped to
  users. `npm audit fix` resolves the non-breaking ones; the `uuid` advisory needs `--force` and
  would downgrade `expo` to 46.0.21 (breaking) — not recommended as a quick fix.
- **Outdated deps:** No dependency more than one major version behind. One major behind each:
  `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1) and `typescript` (6.0.3 → 7.0.2).
  Everything else (expo, react, react-native, react-navigation, supabase-js, react-native-svg,
  etc.) is only patch/minor behind latest.
- **Housekeeping notes:** No TODO/FIXME/XXX/HACK comments under `app/src` or `supabase/functions`.
  No `console.log`/`console.debug`/`debugger` leftovers. No stray `.bak`/`.tmp`/`.orig`/debug
  files in the project tree. No `.env` files present to leak (only the committed `.env.example`).
  `git status` on the project tree is clean.

## Action needed
Nothing in the app itself failed tonight — type check, bundle, and dependency posture are all
healthy and unchanged in character from recent nights. The one open item is procedural: this
session's network policy still blocks Supabase, so the live `check-price` smoke test remains
unverified for today. Worth checking from an environment with real network access to confirm
the backend is actually serving estimates, since this has now been unverified across multiple
consecutive nightly runs.
