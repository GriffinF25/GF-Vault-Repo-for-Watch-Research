# Nightly Health Check — 2026-09-23

## Status: ISSUES FOUND

- **Live backend smoke test: BLOCKED (top-line finding) — not a confirmed app failure.** This
  session's egress proxy denied the connection to `ndjgvmvfgtiuchwlpqed.supabase.co:443`
  (`CONNECT tunnel failed, response 403`, proxy reason: `connect_rejected — organization
  policy`). Both the `/auth/v1/signup` call and a plain health check to that host were rejected
  before reaching Supabase at all — this is the sandbox's network policy blocking the Supabase
  domain from this environment, not evidence the live backend/API is actually down. **A human
  needs to run this check (or unblock the domain for this routine) to get a real pass/fail on
  the live `check-price` function.**
- Type check: PASS — `npx tsc --noEmit` completed with no errors.
- Web bundle: PASS — `npx expo export --platform web` succeeded (Metro bundled 608 modules,
  1.2MB JS bundle, 3 files + 11 assets exported). `dist/` deleted after, not committed.
- npm audit: 23 vulnerabilities (14 moderate, 9 high). All trace to a vulnerable `uuid` (<11.1.1)
  pulled in transitively via `@expo/ngrok` → `xcode` → `@expo/config-plugins` → `@expo/cli` →
  `expo`'s dev-tooling chain (`GHSA-w5hq-g745-h8pq`, buffer bounds check). This is Expo CLI/dev
  dependency surface, not code shipped in the app bundle. `npm audit fix --force` would bump
  `expo` to `46.0.21`, a breaking downgrade from the current `57.0.8` — do not run that
  unattended.
- Outdated deps: two packages more than one major version behind — `typescript` (6.0.3 → latest
  7.0.2) and `@react-native-async-storage/async-storage` (2.2.0 → latest 3.1.1). Everything else
  outdated (`expo`, `react-navigation` packages, `@supabase/supabase-js`, `react-native`, etc.)
  is within a minor/patch bump of latest.
- Housekeeping notes: no TODO/FIXME/XXX comments, `console.log`, or `debugger` statements found
  under `app/src` or `supabase/functions`. No stray `.bak`/`.orig`/debug/tmp/scratch files found
  anywhere in the project tree (outside `node_modules`).
