# Nightly Health Check — 2026-08-24

## Status: ISSUES FOUND

- **Live backend smoke test: FAIL — not executed (top-line finding).** The sandbox's outbound
  proxy again rejected the `CONNECT` to `ndjgvmvfgtiuchwlpqed.supabase.co:443` with a hard 403
  (`curl`: "CONNECT tunnel failed, response 403"; HTTP status `000`). The proxy's own status
  endpoint confirms this was a policy denial (`connect_rejected`, "gateway answered 403 to
  CONNECT (policy denial or upstream failure)") — `supabase.co` is still not in this
  environment's egress allowlist (`noProxy` only covers `anthropic.com`, `registry.npmjs.org`,
  and a handful of other infra hosts). The anon signup call could not be attempted, so
  `check-price` and its `estimate` payload were never reached — no pass/fail signal on the
  backend itself tonight. This is now **29 consecutive nights** (2026-07-27 through tonight)
  this check has never once run. Still an environment allowlist gap, not a confirmed app/backend
  outage — `ndjgvmvfgtiuchwlpqed.supabase.co` (or `*.supabase.co`) needs adding to this scheduled
  task's network policy. The routine cannot fix this itself and will keep reporting it nightly
  until a human changes the environment config.
- **Type check:** PASS — `npx tsc --noEmit` completed with no errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS
  bundle). Generated `dist/` deleted after export, not committed.
- **npm audit:** 19 vulnerabilities (11 moderate, 8 high) — same count and severity split as
  the prior several nights, same underlying advisory set. All are in Expo/Metro/React Native
  dev-tooling chains (`@expo/cli`, `@expo/metro*`, `metro*`, `xcode`, `@expo/ngrok`, `expo`,
  `react-native`), plus transitive `image-size`, `js-yaml`, `nanoid`, `uuid`, `brace-expansion` —
  not app runtime code. Plain `npm audit fix` still doesn't clear the high-severity set; force
  fix still downgrades `expo` to a breaking version — needs human review, not auto-applied.
- **Outdated deps:** Same two one-major-behind packages as prior nights —
  `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1) and `typescript` (6.0.3 → 7.0.2).
  Neither is more than one major version behind. Everything else outdated is a patch/minor bump
  (`@react-navigation/native`, `@react-navigation/native-stack`, `@supabase/supabase-js`,
  `@types/react`, `expo`, `expo-image-picker`, `react`, `react-dom`, `react-native`,
  `react-native-safe-area-context`, `react-native-screens`, `react-native-svg`). Nothing newly
  crossed the one-major-version threshold.
- **Housekeeping notes:** No TODO/FIXME/XXX found in tracked source under `watch-price-app/`
  (the only regex hits were unrelated integrity-hash substrings inside `package-lock.json`, not
  code comments). No stray `console.log`/`console.debug` calls outside
  `supabase/scripts/sync-baselines.ts` (intended CLI logging). No backup/debug/tmp files in the
  tracked tree. `git status` was clean before and after this run.
