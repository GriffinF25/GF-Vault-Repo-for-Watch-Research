# Nightly Health Check — 2026-09-11

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding).** This session's outbound
  network policy blocks the CONNECT to `ndjgvmvfgtiuchwlpqed.supabase.co:443` outright — the
  agent proxy returned a hard 403 ("gateway answered 403 to CONNECT (policy denial or upstream
  failure)") before any request reached Supabase. Confirmed twice directly (`curl -v` to
  `/auth/v1/health`), same result both times. This is an environment/network-policy
  restriction on this scheduled session, not a confirmed failure of the live app or backend —
  the anonymous signup + `check-price` call was never actually attempted, so the real status of
  the live backend is unknown. Per this log directory's history, this exact block has now
  prevented the smoke test on every nightly run since at least 2026-07-27 — 46+ consecutive
  nights — meaning this check has never once confirmed the live backend is healthy in that
  entire span. This needs either (a) `*.supabase.co` added to this scheduled session's egress
  allowlist, or (b) the smoke test moved to a session/environment with unrestricted egress.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS
  bundle, 3 files + 11 assets). Generated `dist/` was deleted after the check per instructions.
- **npm audit:** 26 vulnerabilities (17 moderate, 9 high, 0 critical), all transitive —
  rooted in `@expo/metro`/`metro-config`/`metro-transform-worker`, `js-yaml`, `nanoid`, and
  `uuid` (pulled in via `@expo/ngrok`, `xcode`, `@expo/config-plugins`, `@expo/cli`, `expo`).
  `npm audit fix` covers the `js-yaml`/`nanoid` chain; the `uuid` chain needs
  `npm audit fix --force`, which would downgrade `expo` to `46.0.21` (breaking) — not a safe
  automatic fix while the app is on Expo 57.
- **Outdated deps:** No dependency is more than one major version behind. Closest:
  `@react-native-async-storage/async-storage` 2.2.0 → latest 3.1.1 (one major), `typescript`
  6.0.3 → latest 7.0.2 (one major). Everything else (`expo`, `@react-navigation/native`,
  `@react-navigation/native-stack`, `@supabase/supabase-js`, `react`, `react-dom`,
  `react-native`, `expo-image-picker`, `react-native-svg`, `react-native-screens`,
  `react-native-safe-area-context`, `@types/react`) is only a patch/minor behind.
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX/HACK comments or stray
  `console.log`/`debugger` statements found under `app/src` or `supabase/functions`. No
  backup/debug-leftover files. `git status` was clean before and after this run (dist/ removed,
  nothing else touched).
