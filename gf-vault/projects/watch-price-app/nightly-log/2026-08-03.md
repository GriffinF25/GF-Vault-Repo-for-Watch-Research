# Nightly Health Check — 2026-08-03

## Status: ISSUES FOUND

- **Live backend smoke test: FAIL (not run) — blocked by this environment's network egress policy,
  eighth consecutive night.** The sandbox's outbound proxy returned 403 on `CONNECT` to
  `ndjgvmvfgtiuchwlpqed.supabase.co:443`, so the anon signup + `check-price` function call could
  not be attempted at all (confirmed via `/root/.ccr/README.md`'s guidance: 403/407 is an
  organization egress policy denial, not something to retry or route around). This has now failed
  identically every night since 2026-07-27 — the smoke test has never actually executed in eight
  runs. This reads as an environment allowlist gap, not a confirmed app-side outage, but it also
  means this health check has been providing **zero real signal on whether the live app works**
  for over a week. Needs `ndjgvmvfgtiuchwlpqed.supabase.co` added to this scheduled task's network
  allowlist, or the smoke test moved to an environment that has it.
- **Type check:** PASS — `npx tsc --noEmit` completed with no errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS bundle).
  Generated `dist/` deleted after export, not committed.
- **npm audit:** 11 moderate-severity vulnerabilities, same root cause as prior runs: `uuid <11.1.1`
  pulled in transitively via `@expo/ngrok` → `xcode` → `@expo/config-plugins` → `@expo/cli` → `expo`
  (dev/build tooling, not app runtime code). `npm audit fix` alone doesn't resolve it; `--force`
  would downgrade `expo` to `46.0.21`, a breaking change — needs human judgment, not auto-applied.
- **Outdated deps:** `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1) and `typescript`
  (6.0.3 → 7.0.2) are each one major version behind. Everything else outdated is a patch/minor
  bump (`@supabase/supabase-js`, `expo`, `expo-image-picker`, `react`, `react-dom`, `react-native`,
  `react-native-safe-area-context`, `react-native-svg`, `@types/react`). Nothing is more than one
  major version behind.
- **Housekeeping notes:** No TODO/FIXME/XXX comments, `console.log`, or `debugger` statements found
  under `app/src` or `supabase/functions`. No stray backup/debug/scratch files in the repo tree
  (only the expected `.expo/dev/logs/export.log` build artifact, which is gitignored and untracked).
  `git status` was clean before and after this run. Same doc/code drift as prior runs, still
  unfixed: `README.md` still describes `claude-opus-4-8` as the default price-search model, but
  `supabase/functions/check-price/index.ts` already uses `claude-sonnet-5`.
