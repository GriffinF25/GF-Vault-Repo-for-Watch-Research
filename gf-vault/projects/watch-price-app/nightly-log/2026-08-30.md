# Nightly Health Check — 2026-08-30

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding).** This sandbox's outbound
  network egress policy blocks the CONNECT to `ndjgvmvfgtiuchwlpqed.supabase.co` outright (403
  policy denial at the proxy — `recentRelayFailures` shows `connect_rejected` before any
  request reached Supabase), so the anonymous signup + `check-price` call could not be
  attempted tonight. Per proxy guidance, this class of failure is not retried or routed around.
  This is an environment restriction on this nightly-check session, not evidence the live app
  or Edge Function is broken. Per this log directory's history, this exact block has now
  prevented the live smoke test on every nightly run since at least 2026-07-27 (30+ consecutive
  nights) — the check has never once confirmed the live backend is healthy in that span. This
  needs either (a) a manual run of the smoke test from an unrestricted environment, or (b) an
  update to this session's egress allowlist to include the Supabase project host, since as
  configured this step of the nightly check cannot ever pass.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules bundled,
  1.2MB JS bundle). Generated `dist/` deleted after the check, not committed.
- **npm audit:** 19 vulnerabilities (11 moderate, 8 high, 0 critical) — same count/severity
  split as prior weeks. Findings trace to the Expo CLI/Metro dev-and-build toolchain (`@expo/
  metro`, `metro`, `metro-config`, `metro-transform-worker`, `js-yaml`, `nanoid`, plus moderate
  findings in `@expo/cli`/`@expo/config-plugins`/`@expo/ngrok`/`xcode`/`uuid`), not app runtime
  code. Plain `npm audit fix` doesn't clear the high-severity set; `npm audit fix --force` would
  downgrade `expo` to a breaking pre-40 version — not attempted, still needs human review.
- **Outdated deps:** None more than one major version behind. Exactly one major behind:
  `typescript` 6.0.3 → 7.0.2, `@react-native-async-storage/async-storage` 2.2.0 → 3.1.1.
  Everything else outdated is a minor/patch bump (`expo` 57.0.8→57.0.18, `@supabase/supabase-js`
  2.110.8→2.112.4, `react-native` 0.86.0→0.87.1, `@react-navigation/native` 7.3.14→7.3.18,
  `@react-navigation/native-stack` 7.18.6→7.18.10, `react`/`react-dom` 19.2.3→19.2.8,
  `expo-image-picker` 57.0.6→57.0.14, `react-native-screens` 4.26.2→4.26.2, `react-native-svg`
  15.15.4→15.15.5, `react-native-safe-area-context` 5.7.0→5.9.1, `@types/react` 19.2.17→19.2.18).
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX/HACK comments or `console.log`/`debugger`
  left in `app/src` or `supabase/functions`. No stray debug/backup/tmp files in the project
  (only match outside `node_modules` was `app/.expo/dev/logs/export.log`, a gitignored
  build-cache artifact produced by this run's own `expo export`, not a pre-existing leftover).
