# Nightly Health Check — 2026-08-11

## Status: ISSUES FOUND

- **Live backend smoke test: FAIL — not executed (top-line finding).** The sandbox's outbound
  proxy rejected the `CONNECT` to `ndjgvmvfgtiuchwlpqed.supabase.co:443` with a hard 403
  (`curl` verbose output: "CONNECT tunnel failed, response 403"; confirmed independently via the
  proxy's own status endpoint, which logs it as `connect_rejected` — "gateway answered 403 to
  CONNECT (policy denial or upstream failure)"). The anon signup call could not even be attempted,
  so the `check-price` function and its `estimate` payload were never reached — no pass/fail
  signal either way. This is now **16 consecutive nights** (2026-07-27 through tonight) this
  check has never once run. This remains an environment allowlist gap —
  `ndjgvmvfgtiuchwlpqed.supabase.co` (or `*.supabase.co`) needs adding to this scheduled task's
  network policy — not a confirmed app/backend outage. A human needs to change the environment
  config; the routine cannot fix this itself and will keep reporting it nightly until that
  happens.
- **Type check:** PASS — `npx tsc --noEmit` completed with no errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules, 1.2MB JS
  bundle, matches prior nights). Generated `dist/` deleted after export, not committed.
- **npm audit:** 22 vulnerabilities (8 moderate, 14 high) — unchanged from 2026-08-10. All are in
  Expo/Metro/React Native dev-tooling chains (`@expo/cli`, `@expo/metro*`, `metro*`,
  `@react-native/community-cli-plugin`, `xcode`, `uuid`), not app runtime code. Same notable
  high-severity entries as before: `image-size` (DoS via infinite loop in ICNS/JXL/HEIF parsing),
  `js-yaml` (quadratic CPU DoS, CVE-2026-59870 fix not backported), `nanoid` (infinite loop with
  size-zero custom generator), `brace-expansion` (DoS via unbounded intermediate arrays). `npm
  audit fix --force` is available but downgrades `expo` (breaking) — still needs human review,
  not auto-applied.
- **Outdated deps:** Same two one-major-behind packages as prior nights —
  `@react-native-async-storage/async-storage` (2.2.0 → 3.1.1) and `typescript` (6.0.3 → 7.0.2).
  Neither is more than one major version behind. Everything else outdated is a patch/minor bump
  (`@supabase/supabase-js`, `expo`, `expo-image-picker`, `react`, `react-dom`, `react-native`,
  `react-native-safe-area-context`, `react-native-screens`, `react-native-svg`, `@types/react`,
  `@react-navigation/native`, `@react-navigation/native-stack`). Nothing newly crossed the
  one-major-version threshold.
- **Housekeeping notes:** No TODO/FIXME/XXX/HACK, `console.log`, or `debugger` found under
  `app/src` or `supabase/functions`. No stray backup/debug/tmp files in the tracked repo tree
  (the only debug-looking artifact was `app/.expo/dev/logs/export.log`, a gitignored build cache
  file produced by tonight's own `expo export` run, not committed). `git status` was clean before
  and after this run. Same doc/code drift as prior runs, still unfixed: `README.md` (line 100)
  still describes `claude-opus-4-8` as the default price-search model, but
  `supabase/functions/check-price/index.ts` (`MODEL` constant, line 15) already uses
  `claude-sonnet-5`.
