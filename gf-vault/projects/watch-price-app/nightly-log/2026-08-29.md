# Nightly Health Check — 2026-08-29

## Status: ISSUES FOUND

- **Live backend smoke test: NOT EXECUTED (top-line finding).** This sandbox's outbound
  network policy blocks the CONNECT to `ndjgvmvfgtiuchwlpqed.supabase.co` outright (403 policy
  denial at the proxy, before any request reached Supabase), so the anonymous signup +
  `check-price` call could not be attempted tonight. This is an environment restriction on
  this nightly-check session, not evidence the live app or Edge Function is broken. Per the
  history in this log directory, this exact block has now prevented the live smoke test on
  every single nightly run since at least 2026-07-27 (30+ consecutive nights) — the check has
  never once confirmed the live backend is healthy in that span. This needs either (a) a
  manual run of the smoke test from an unrestricted environment, or (b) an update to this
  session's egress allowlist, since as configured this step of the nightly check cannot ever
  pass.
- **Type check:** PASS — `npx tsc --noEmit` completed with zero errors.
- **Web bundle:** PASS — `npx expo export --platform web` succeeded (608 modules bundled,
  1.2MB JS bundle). Generated `dist/` deleted after the check, not committed.
- **npm audit:** 19 vulnerabilities (11 moderate, 8 high, 0 critical) — same count and severity
  split as the prior several weeks, same underlying advisory set (`@expo/metro`, `metro`,
  `metro-config`, `metro-transform-worker`, `brace-expansion`, `image-size`, `js-yaml`,
  `nanoid`, plus moderate findings in `@expo/cli`/`@expo/config-plugins`/`@expo/ngrok`/`xcode`/
  `uuid`). All trace back to the Expo CLI/Metro dev-and-build toolchain, not app runtime code.
  Plain `npm audit fix` doesn't clear the high-severity set; `npm audit fix --force` would
  downgrade `expo` to a breaking pre-40 version — not attempted, still needs human review.
- **Outdated deps:** None more than one major version behind. Exactly one major behind:
  `typescript` 6.0.3 → 7.0.2, `@react-native-async-storage/async-storage` 2.2.0 → 3.1.1.
  Everything else outdated is a minor/patch bump (`expo` 57.0.8→57.0.18, `@supabase/supabase-js`
  2.110.8→2.112.4, `react-native` 0.86.0→0.87.1, `@react-navigation/*`, `react`/`react-dom`
  19.2.3→19.2.8, `expo-image-picker`, `react-native-screens`, `react-native-svg`,
  `react-native-safe-area-context`).
- **Housekeeping notes:** Clean. No TODO/FIXME/XXX/HACK comments in `app/src` or
  `supabase/functions`. No stray debug/backup/tmp files in the project (only match outside
  `node_modules` was `app/.expo/dev/logs/export.log`, a gitignored build-cache artifact
  produced by this run's own `expo export`, not a pre-existing leftover). `console.log` /
  `console.error` usage in `supabase/functions` is limited to intentional error logging
  (`console.error` on caught exceptions), nothing left over from debugging.
